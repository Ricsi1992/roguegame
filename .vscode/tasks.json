{
    "tasks": [
        {
            "label": "cmake generate and build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}\\build"
            },
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug -G \"MinGW Makefiles\" ${workspaceRoot} && mingw32-make"
        }
    ],
    
    "version": "2.0.0"
}